# 归档层

这里放已经退役的历史发布材料。

当前没有迁入旧发布材料。原 `archive/` 目录中的 V1/V2/V3/V4 文件被重新判断为“探讨材料”，已迁入 `docs/discovery/`，因为它们仍然解释当前方案的形成过程。

## 归档标准

- 曾经作为 `latest/` 使用过。
- 已被新版本替代。
- 不再需要参与当前决策，但需要保留历史记录。

## 不归档的内容

- 仍用于解释当前判断的方案推演，放入 `discovery/`。
- 当前迭代草案，放入 `working/`。
- 已冻结发布快照，保留在 `releases/`。